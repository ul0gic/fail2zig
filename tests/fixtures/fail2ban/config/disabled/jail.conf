[DEFAULT]
bantime = 10m
findtime = 10m
maxretry = 5
backend = auto
banaction = nftables-allports
action = %(banaction)s

[sshd]
enabled = false
logpath = /var/log/auth.log
operator_note = kept for later review

[nginx-http-auth]
logpath = /var/log/nginx/error.log
